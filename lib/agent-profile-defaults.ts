/**
 * Seeds for newly created agents so public profiles aren't empty.
 * Keep strings aligned with SPECIALTY_OPTIONS / LANGUAGE_OPTIONS in agent-dashboard.tsx.
 */

export const DEFAULT_AGENT_LANGUAGES_COMMAS = "English, Filipino";

export const DEFAULT_AGENT_SPECIALTIES_COMMAS = "Condo, Rental";

/** Public `/agents/[id]` sidebar tagline (≤ PROFILE_TAGLINE_MAX in page + API). */
export const DEFAULT_AGENT_BIO_TAGLINE = "Happily guiding your real-estate moves.";
